
import { useBillingStore } from '../stores/billingStore';

/**
 * Hook Alias: Provides access to the Billing Store.
 * Restored for modular access and backward compatibility.
 */
export const useBilling = useBillingStore;
