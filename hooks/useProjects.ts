
import { useProjectStore } from '../stores/projectStore';

/**
 * Hook Alias: Provides access to the Project Store.
 * Restored for modular access and backward compatibility.
 */
export const useProjects = useProjectStore;
