
import { useDirectorStore } from '../stores/directorStore';

/**
 * Hook Alias: Provides access to the Director Store.
 * Restored for modular access and backward compatibility.
 */
export const useDirector = useDirectorStore;
