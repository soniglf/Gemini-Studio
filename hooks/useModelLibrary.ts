
import { useModelStore } from '../stores/modelStore';

/**
 * Hook Alias: Provides access to the Model Store.
 * Restored for modular access and backward compatibility.
 */
export const useModelLibrary = useModelStore;
